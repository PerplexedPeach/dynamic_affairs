# stamina (how long they can last without climaxing)
# expects character scope
max_sex_stamina = {
	value = 5
	if = {
		limit = { has_trait = stubborn }
		add = 2
	}
	# everyone has health around 4, so we take the offset and magnify it with respect to 2
	add = {
		add = health
		subtract = 4
		multiply = 2
	}
	min = 1
}

dom_tolerance = {
	
	#additions
	value = 10
	if = {
		limit = { has_trait = stubborn }
		add = 3
	}
	if = {
		limit = { has_trait = arrogant }
		add = 4
	}
	if = {
		limit = { has_trait = wrathful }
		add = 5
		}
	if = {
		limit = {
			OR = {
				has_trait = sadistic
				has_trait = callous
				}
			} 
		add = 6
	}

	#subtractions
	if = {
		limit = { has_trait = craven }
		subtract = 3
	}
	if = {
		limit = { has_trait = shy }
		subtract = 2
	}
	if = {
		limit = { has_trait = trusting }
		subtract = 2
	}

	#TODO: add for LIDA traits

	min = 0
}

# Todo : after adding for LIDA traits copy for sub. 